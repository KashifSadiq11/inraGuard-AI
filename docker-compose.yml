version: "3.9"

services:
  infraguard:
    build: .
    container_name: infraguard-ai
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped

  # Optional: Ollama as a local LLM backend (comment out if using Groq)
  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: ollama
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama_data:/root/.ollama
  #   restart: unless-stopped

# volumes:
#   ollama_data:
